# 005-warranty-callback 域歧义正向固化(Clarify)

> 日期: 2026-07-09
> 固化歧义数: 18
> 数据来源: `docs/reverse-engineering/005-warranty-callback/ambiguities.md`
> 固化原则: 逐条采纳 ambiguities.md 的"建议决策"作为最终决策,形成四字段记录(决策结论 / 依据 / 影响范围 / 回滚提示)。
> 比对类型:A=代码 vs 文档;B=代码 vs 代码;C=spec 内部 [待澄清] 展开

---

## 一、spec 内部 [待澄清] 展开(C 类)

### AMB-005-01: 问卷模板三表表名为推断,未经 DML 确认

- **决策结论**: [暂定决策:采纳当前推断表名 `pm_closed_loop_quesnaire` / `pm_closed_loop_quesnaire_line` / `pm_closed_loop_quesnaire_opt` 作为规范表名;后续正向工程前需检索 `PmClosedLoopQuesnaire*` 对应的 iBatis sql-map 配置或数据库 DDL 脚本最终确认,若与推断不符则回写 spec]
- **依据**: ambiguities.md 建议决策——"检索 `PmClosedLoopQuesnaire*` 对应的 iBatis sql-map 配置或数据库 DDL 脚本,确认精确表名后回写 spec"。当前实体 Bean 与 Service 引用已存在,DML 未直接暴露,推断名具备代码层佐证。
- **影响范围**: spec.md 3.7 / 3.8 / 3.9 数据契约;数据契约首段注释;后续正向工程 DDL 生成;DATA-REUSE-01 复用判断。
- **回滚提示**: 若 DDL 脚本确认真实表名与推断不符,需同步更新 spec.md 3.7/3.8/3.9 表名、移除"[表名为推断]"标注,并复核所有引用点的 resultMap/别名映射。

### AMB-005-02: 两套回访体系并存关系与数据归并语义不明

- **决策结论**: [暂定决策:确认两套回访体系并存服役,业务边界定义为——`pm_cl_callback`(独立回访工作流 CallBack.bpmn)服务于"项目经理发起的独立回访申请审批流";`pm_cl_evaluation_header`(PM 闭环内嵌回访节点 evaluationType=3)服务于"PM 闭环流程内的回访环节"。两套数据不迁移、不归并,统计口径需按数据来源分区统计,禁止跨体系混算;同一项目可分别在两套体系各产生回访记录,需在统计视图中标注来源体系避免重复计数]
- **依据**: ambiguities.md 建议决策——"明确两套体系的业务边界;若并存则定义数据归并策略"。两套流程各自挂独立 BPMN 与申请单/评测头,字段结构差异较大,迁移成本高且无业务驱动力,故并存并分区统计。
- **影响范围**: spec.md FR-2.2 vs FR-2.3;3.2 `pm_cl_callback` vs 3.4 `pm_cl_evaluation_header`;Edge Cases;User Story 6 统计口径;SC-011 计分一致性。
- **回滚提示**: 若后续业务决定废弃独立回访工作流,需对 `pm_cl_callback` 及其关联表标注 deprecation,规划历史数据迁移至评测头体系,并调整统计视图口径。

### AMB-005-03: 续保意向 renewalIntention 值 3 语义不明

- **决策结论**: [暂定决策:值 3 显式标注为"已废弃枚举(历史'已续保'语义,当前无写入路径)";spec 枚举仅保留 0否/1有/2待定(-1 未填写仅查询用);`canEdit()` 中"续保状态=3"逻辑已禁用,不再纳入有效取值域]
- **依据**: ambiguities.md 建议决策——"检索 `renewalIntention=3` 的写入路径;若废弃则在 spec 枚举中显式标注'3=已废弃'"。当前 `canEdit` 恒返回 false 已禁用该路径,且枚举文档无值 3 定义,判定为历史遗留。
- **影响范围**: spec.md 3.1 `renewalIntention`;FR-2.1.5;FR-2.1.11 `canEdit()`;Open Questions 第 3 项。
- **回滚提示**: 若后续确认值 3 仍由外部域合法写入,需恢复枚举定义并补充语义说明,同时复核 `canEdit` 是否需重新启用对应分支。

### AMB-005-04: 闭环网关 evaluationResult=2/3/-2 无常量定义

- **决策结论**: [暂定决策:值 2/3/-2 明确标注为"流程内部网关态,非评测结果业务枚举",由 BPMN 网关表达式在流程流转过程中计算得出,不写入 `pm_cl_evaluation_header.evaluationResult` 业务字段;业务枚举仍以 FR-2.3.7 的 1=同意/-1=不同意/-3=无法回访 为准。网关态语义:2=已通过回访(进入工程评分前置判断)、3=服务经理与项目经理一致且通过回访、-2=驳回任务办理(结束)]
- **依据**: ambiguities.md 建议决策——"结合 PmClosedLoop.bpmn 与服务实现确认 2/3/-2 的产生路径;补充常量定义,或在 spec 中明确标注为'流程内部网关态,非评测结果枚举'"。常量类 `PmClosedLoopConstant` 仅定义 1/-1/-3,2/3/-2 仅在 BPMN 网关表达式中出现,符合流程内部态特征。
- **影响范围**: spec.md FR-2.3.7;FR-2.3.10;`PmClosedLoopConstant.java`;Open Questions 第 4 项;Edge Cases。
- **回滚提示**: 若后续服务实现确认 2/3/-2 实际写入业务字段,需补充常量定义并扩展 FR-2.3.7 枚举,同时复核网关条件与业务结果的一致性。

### AMB-005-05: pmClosedLoopResultType 41/42/50/51 注释存在但代码未使用

- **决策结论**: [暂定决策:spec 仅记录实际生效的取值 0=流程图查看、1=提交问卷并计分、2=基于已有问卷完成节点、30=回访表单(入口取表单/提交后跳看分页)、40=闭环表单(入口取表单/提交后跳看分页);41/42/50/51 标注为"注释残留,代码未实现,视为废弃"]
- **依据**: ambiguities.md 建议决策——"确认 41/42/50/51 是否废弃;在 spec 中仅记录实际生效的 0/1/2/30/40 并标注注释残留"。代码分支仅出现 0/1/2/30/40,41/42/50/51 无任何处理路径。
- **影响范围**: spec.md FR-2.3.3 / FR-2.3.5 / FR-2.3.19;`PmClosedLoopAction.java:43` 字段注释;Open Questions 第 5 项;Edge Cases。
- **回滚提示**: 若后续前端传入 41/42/50/51 且需后端支持,需补充对应分支处理逻辑并更新 spec 取值表;在此之前后端对未知值应按非法参数拒绝。

---

## 二、代码 vs 代码(B 类)

### AMB-005-06: "无法回访"结果值在两套流程中不一致(-2 vs -3)

- **决策结论**: [暂定决策:两套流程值域隔离,spec 显式分区标注——独立回访工作流(CallBack.bpmn,FR-2.2.8)"无法回访"= -2;PM 闭环流程(PmClosedLoop.bpmn,FR-2.3.7)"无法回访"= -3。两值域不混用,跨流程统计时需按来源流程映射到统一语义标签而非统一数值]
- **依据**: ambiguities.md 建议决策——"统一'无法回访'枚举值;若两套流程值域隔离则需在 spec 中显式分区标注,避免混用"。两套流程独立演进且常量类已固化不同值,统一改动影响面大,故采取分区标注策略。
- **影响范围**: spec.md FR-2.2.8 vs FR-2.3.7;`PmClosedLoopConstant.java:105`;跨流程结果统计口径。
- **回滚提示**: 若后续推进枚举统一,需同步修改 CallBack.bpmn 网关条件与独立回访流程服务实现,将 -2 改为 -3(或反之),并迁移历史数据。

### AMB-005-07: pmClosedLoopResultType=1/2 按角色重载,语义随节点变化

- **决策结论**: [暂定决策:在 spec 中明确"=1/=2 语义依赖当前节点角色与流程上下文"——回访人员节点 :=1 提交回访问卷并计分、=2 基于已有问卷结果完成回访节点;工程人员节点 :=1 提交闭环建议问卷并计分、=2 完成闭环节点。办理时由当前流程节点上下文区分语义,不做参数拆分;长期建议拆分为独立参数以提升可读性]
- **依据**: ambiguities.md 建议决策——"在 spec 中明确'=1/=2 语义依赖当前节点角色'的上下文依赖关系;建议长期拆分"。当前代码靠流程节点上下文区分,功能可用,短期拆分成本高。
- **影响范围**: spec.md FR-2.3.3 vs FR-2.3.5;`PmClosedLoopAction.java:545,604,698`;接口契约可读性;测试覆盖。
- **回滚提示**: 长期拆分时需新增独立参数(如 `submitQuestionnaire`/`completeNode`),调整前端调用与后端分支,并保留旧参数兼容期。

### AMB-005-08: pmClosedLoopResultType=30/40 入口态与结果态复用

- **决策结论**: [暂定决策:在 spec 中补充 30/40 的双态语义说明——入口态:进入页面时取回访(30)/闭环(40)表单数据;结果态:提交成功后置值并跳转看分页。同一值在流程不同阶段含义不同,靠流程位置与返回视图区分]
- **依据**: ambiguities.md 建议决策——"在 spec 中补充 30/40 的双态语义说明(入口取表单 / 提交后跳看分页)"。代码中 30/40 在入口与提交后均出现,属状态机复用。
- **影响范围**: spec.md FR-2.3.3 / FR-2.3.5 / FR-2.3.19;`PmClosedLoopAction.java:108,114,601,742`;前端跳转逻辑。
- **回滚提示**: 若后续区分入口态与结果态,需引入新参数或状态字段,调整前端跳转与后端视图返回逻辑。

### AMB-005-09: 问卷计分逻辑三处实现,存在分叉风险

- **决策结论**: [暂定决策:统一收敛到 `util/QuestionnarieUtil`;在 spec 中标注独立回访(CallBackAction)与闭环流程(PmClosedLoopAction)当前为双实现,需增加跨流程一致性测试用例保障 SC-011;新增/变更计分规则必须先改 Util,再由两处调用,禁止直接修改 Action 内联逻辑]
- **依据**: ambiguities.md 建议决策——"统一收敛到 `QuestionnarieUtil`;在 spec 中标注双实现并增加跨流程一致性测试用例"。三处实现威胁 SC-011(计分一致性 100%),需强制收敛。
- **影响范围**: spec.md SC-011;`CallBackAction.java:344`;`PmClosedLoopAction.java:847`;`util/QuestionnarieUtil.java:52`;FR-2.2.5 / FR-2.3.17。
- **回滚提示**: 收敛过程需保留旧实现作为对照测试基线,确认 Util 输出与旧实现完全一致后方可移除内联逻辑;若发现历史差异需评估是否为 bug 修正。

### AMB-005-10: 邮件模板 ID 27 缺失,范围描述"20-28"具有误导性

- **决策结论**: [暂定决策:在 spec 中枚举实际 8 个邮件模板 ID:20,21,22,23,24,25,26,28(ID 27 缺失);标注"ID 27 归属待排查,闭环域未使用";移除"模板 ID 20-28"的连续范围描述]
- **依据**: ambiguities.md 建议决策——"在 spec 中枚举实际 8 个 ID 并标注 27 缺失;排查 ID 27 归属"。常量类 `CL_MAIL_TEMPLATE_01..08` 实际 ID 不连续,文档描述需与代码对齐。
- **影响范围**: spec.md FR-2.3.16;SC-007;`PmClosedLoopConstant.java:170-205`;邮件模板配置核验。
- **回滚提示**: 若排查确认 ID 27 属于闭环域且需启用,需补充常量定义与模板内容,并更新 spec 枚举。

### AMB-005-11: 状态常量命名与语义不匹配(SUBMIT=生效)

- **决策结论**: [暂定决策:在 spec 中以中文语义为准,标注常量名与语义的映射矛盾——`CL_STATUS_SUBMIT=1` 常量名为 SUBMIT 但实际语义为"生效"(历史命名遗留,实指"提交后生效");`CL_STATUS_SUBMITQUES=2` 语义为"提交问卷"。明确 FR-2.3.8 所述"问卷状态(含 2=提交问卷)"归属实体为评测头 `pm_cl_evaluation_header.status`(3.4),而非模板头 `questionnaireStatus`(3.7,仅 1/-1/-2 三态)]
- **依据**: ambiguities.md 建议决策——"在 spec 中以中文语义为准,标注常量名与语义的映射矛盾;明确 FR-2.3.8 归属实体(评测头 vs 模板)"。FR-2.3.8 取值含 2=提交问卷,与评测头 status 一致,模板头无此态。
- **影响范围**: spec.md FR-2.3.8;3.4 `pm_cl_evaluation_header.status`;3.7 `questionnaireStatus`;`PmClosedLoopConstant.java:48,63`。
- **回滚提示**: 若后续重命名常量,需全局替换 `CL_STATUS_SUBMIT` 引用并更新注释,避免破坏现有逻辑。

### AMB-005-12: evaluationType=2"服务经理回访申请"触发时机不明

- **决策结论**: [暂定决策:确认 evaluationType=2 由服务经理审核节点产生(服务经理审核即生成 type=2 评测头,命名"回访申请"指其触发后续回访环节,与服务经理审核为同一节点的评测头记录);spec 补充说明"type=2 在服务经理审核节点生成,命名为'回访申请'因其触发后续回访,并非独立子流程"]
- **依据**: ambiguities.md 建议决策——"结合服务实现确认 type=2 的触发条件并在 spec 补充;统一'审核'与'回访申请'的命名"。User Story 3 流程节点中服务经理审核后即进入回访,type=2 评测头为该节点产物。
- **影响范围**: spec.md FR-2.3.6;`PmClosedLoopConstant.java:74`;评测头类型语义;历史回访查询过滤条件。
- **回滚提示**: 若后续确认 type=2 为独立子流程节点,需更新 spec 流程节点描述并补充触发条件。

---

## 三、代码 vs 文档(A 类)

### AMB-005-13: customInfo 为 null 时增量合并行为未定义

- **决策结论**: [暂定决策:明确规则——customInfo 为 null 时(首次创建场景),增量合并等价于整体写入,即直接将传入 JSON 作为 customInfo 值存储;非 null 时按 JSON_MERGE_PATCH 语义增量合并]
- **依据**: ambiguities.md 建议决策——"在 spec 中明确'customInfo 为 null 时增量合并等价于整体写入'的规则"。JSON_MERGE_PATCH 对 null 文档等价于整体写入,符合语义。
- **影响范围**: spec.md Edge Cases;3.1 `customInfo`;FR-2.1.10;SC-010;Assumptions(JSON_MERGE_PATCH)。
- **回滚提示**: 若数据库 JSON 合并函数对 null 行为与预期不符,需在代码层特判 null 转 empty object 后再合并。

### AMB-005-14: "取首份"生效模板的排序规则未定义

- **决策结论**: [暂定决策:明确排序规则——按生效时间(effectiveStartTime)倒序取最新一份生效的 `projectWarrantyCallback` 类型问卷模板作为"首份"]
- **依据**: ambiguities.md 建议决策——"在 spec 中明确排序规则(建议按生效时间倒序取最新)"。按生效时间倒序保证取到最近生效的模板,符合业务期望。
- **影响范围**: spec.md FR-2.1.4;User Story 1 Acceptance Scenario 2;多模板并存场景。
- **回滚提示**: 若业务确认应按 id 升序取首(数据库默认),需调整排序规则并复核历史数据取模板结果。

### AMB-005-15: questionStatus 取值未文档化

- **决策结论**: [暂定决策:questionStatus 复用模板头状态枚举——1=生效、-1=草稿、-2=失效;spec 补充取值说明]
- **依据**: ambiguities.md 建议决策——"补充 questionStatus 取值枚举,或标注'复用模板头状态枚举'"。题目生命周期与模板一致,复用枚举避免重复定义。
- **影响范围**: spec.md 3.8 `pm_closed_loop_quesnaire_line.questionStatus`;题目生命周期管理;查询过滤条件。
- **回滚提示**: 若后续确认 questionStatus 为独立枚举(如启用/停用),需补充独立取值定义并调整查询逻辑。

### AMB-005-16: quesnaireState 在 pm_cl_callback_quesnaire 仅"1已提交",与 pm_project_warranty_callback 不一致

- **决策结论**: [暂定决策:确认草稿态问卷不写入 `pm_cl_callback_quesnaire` 关联表(仅提交后才建立关联);spec 标注取值域差异原因——3.3 关联表 quesnaireState 仅"1已提交"是因为草稿态不建立关联,3.1 维保回访记录 quesnaireState 含 -1草稿/1已提交是因为维保回访草稿态直接存于主表]
- **依据**: ambiguities.md 建议决策——"确认草稿态是否写入 `pm_cl_callback_quesnaire`;在 spec 中标注取值域差异原因"。关联表设计为提交后关联,草稿态不污染关联关系。
- **影响范围**: spec.md 3.3 `quesnaireState` vs 3.1 `quesnaireState`;草稿问卷写入策略;跨表状态对齐。
- **回滚提示**: 若后续业务要求草稿态也建立关联,需扩展 3.3 取值域并调整关联表写入时机。

### AMB-005-17: FR-2.2.5"每次保存重新生成"与 FR-2.2.6"唯一确定"存在张力

- **决策结论**: [暂定决策:明确重复保存为 upsert(覆盖)语义——同一 `callBackId + taskId` 下每次保存/提交按唯一键 upsert 覆盖旧问卷结果,不保留历史版本;FR-2.2.5"重新生成"指重新计算并覆盖结果数据,FR-2.2.6"唯一确定"指唯一键约束。在 FR-2.2.5/2.2.6 中消解表述冲突]
- **依据**: ambiguities.md 建议决策——"明确重复保存为 upsert(覆盖)还是版本化新增;在 FR-2.2.5/2.2.6 中消解表述冲突"。`callBackId + taskId` 唯一约束语义指向 upsert,版本化需额外版本字段(当前无)。
- **影响范围**: spec.md FR-2.2.5 / FR-2.2.6;Edge Cases"同一回访单不同任务节点维护多份问卷";问卷结果历史保留策略。
- **回滚提示**: 若业务要求版本化保留,需新增版本号字段并调整唯一约束为 (callBackId, taskId, version)。

### AMB-005-18: canEdit"接听状态以-开头"取值来源不明

- **决策结论**: [暂定决策:在 spec 中标注——`canEdit()` 中"接听状态以 - 开头时可编辑"逻辑已禁用(恒返回 false);接听状态取值来源为基础数据 `projectWarrantyCallback_phoneAnswerState`,是否含"-"前缀值待基础数据域确认;在恢复编辑功能前需先完成基础数据枚举核对]
- **依据**: ambiguities.md 建议决策——"确认接听状态基础数据是否含'-'前缀值;在 spec 中标注该逻辑已禁用且取值来源待确认"。当前逻辑已注释禁用,不影响线上行为。
- **影响范围**: spec.md FR-2.1.11;`canEdit()` 逻辑;接听状态枚举完整性;未来恢复编辑功能可行性。
- **回滚提示**: 若后续需恢复 canEdit 编辑功能,需先核对基础数据枚举,确认"-"前缀值语义后再启用对应分支。

---

## 汇总统计

| 比对类型 | 数量 | 编号 |
|---------|------|------|
| C=spec 内部 [待澄清] 展开 | 5 | AMB-005-01 ~ 05 |
| B=代码 vs 代码 | 7 | AMB-005-06 ~ 12 |
| A=代码 vs 文档 | 6 | AMB-005-13 ~ 18 |
| **合计** | **18** | |

### 关键决策(影响面最大,最多 3 条)

1. **AMB-005-02**(两套回访体系并存):确认两套体系并存服役,业务边界按"独立回访申请审批"vs"PM 闭环内嵌回访"划分,统计口径分区统计禁止混算——奠定回访域数据一致性基础。
2. **AMB-005-04**(网关 evaluationResult=2/3/-2):标注为流程内部网关态,非评测结果业务枚举,业务枚举仍以 1/-1/-3 为准——保障 PM 闭环流程流转正确性与网关条件可维护性。
3. **AMB-005-09**(计分逻辑三处实现):统一收敛到 `QuestionnarieUtil`,标注双实现并增加跨流程一致性测试用例——直接保障 SC-011(计分一致性 100%)达成。
