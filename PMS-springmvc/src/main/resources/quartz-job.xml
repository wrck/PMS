<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN//EN" "http://www.springframework.org/dtd/spring-beans-2.0.dtd">

<beans>
	<!-- ============================= Spring Quartz ================================ -->
	<!-- 要调用的工作类 -->
	<bean id="mailJob" class="com.dp.plat.core.schedule.MailerJob"></bean>
	<!-- 定义调用对象和调用对象的方法 -->
	<bean id="mailTask"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<!-- 调用的类 -->
		<property name="targetObject">
			<ref bean="mailJob" />
		</property>
		<!-- 调用类中的方法 -->
		<property name="targetMethod">
			<value>execute</value>
		</property>
		<property name="concurrent" value="true" /><!-- 控制定时任务单线程执行，防止上次同步未执行完，本次又开始执行 -->
	</bean>
	<!-- 定义触发时间 -->
	<bean id="mailTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail">
			<ref bean="mailTask" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<!-- <value>0 0/50 * * * ?</value> -->
			<value>0 0/5 8-20 * * ?</value>
		</property>
	</bean>
	
	<!-- 要调用的工作类 -->
	<bean id="ehrDataJob" class="com.dp.plat.ehr.job.EhrDataJob"></bean>
	<!-- 定义调用对象和调用对象的方法 -->
	<bean id="ehrDataTask"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<!-- 调用的类 -->
		<property name="targetObject">
			<ref bean="ehrDataJob" />
		</property>
		<!-- 调用类中的方法 -->
		<property name="targetMethod">
			<value>execute</value>
		</property>
		<property name="concurrent" value="true" /><!-- 控制定时任务单线程执行，防止上次同步未执行完，本次又开始执行 -->
	</bean>
	<!-- 定义触发时间 -->
	<bean id="ehrDataTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail">
			<ref bean="ehrDataTask" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<!-- <value>0 0/50 * * * ?</value> -->
			<value>0 0/5 8-20 * * ?</value>
		</property>
	</bean>
	
	<!-- 要调用的工作类 -->
	<bean id="sseDispatchPaymentJob" class="com.dp.plat.pms.springmvc.job.DispatchSettlementSEEPaymentJob"></bean>
	<!-- 定义调用对象和调用对象的方法 -->
	<bean id="sseDispatchPaymentTask"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<!-- 调用的类 -->
		<property name="targetObject">
			<ref bean="sseDispatchPaymentJob" />
		</property>
		<!-- 调用类中的方法 -->
		<property name="targetMethod">
			<value>execute</value>
		</property>
		<property name="concurrent" value="true" /><!-- 控制定时任务单线程执行，防止上次同步未执行完，本次又开始执行 -->
	</bean>
	<!-- 定义触发时间 -->
	<bean id="sseDispatchPaymentTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail">
			<ref bean="sseDispatchPaymentTask" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<!-- <value>0 0/50 * * * ?</value> -->
			<value>0 0/5 8-20 * * ?</value>
		</property>
	</bean>
	
	<!-- 要调用的工作类 -->
	<bean id="smsDataJob" class="com.dp.plat.pms.springmvc.job.SMSDataJob"></bean>
	<!-- 定义调用对象和调用对象的方法 -->
	<bean id="smsDataTask"
		class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<!-- 调用的类 -->
		<property name="targetObject">
			<ref bean="smsDataJob" />
		</property>
		<!-- 调用类中的方法 -->
		<property name="targetMethod">
			<value>execute</value>
		</property>
		<property name="concurrent" value="true" /><!-- 控制定时任务单线程执行，防止上次同步未执行完，本次又开始执行 -->
	</bean>
	<!-- 定义触发时间 -->
	<bean id="smsDataTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail">
			<ref bean="smsDataTask" />
		</property>
		<!-- cron表达式 -->
		<property name="cronExpression">
			<!-- <value>0 0/50 * * * ?</value> -->
			<value>0 0/5 8-20 * * ?</value>
		</property>
	</bean>
	
	<!-- 总管理类 如果将lazy-init='false'那么容器启动就会执行调度程序 -->
	<bean id="startQuartz" lazy-init="false" autowire="no"
		class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
			    <!-- <ref bean="smsDataTrigger" />
				<ref bean="sseDispatchPaymentTrigger" />
				<ref bean="ehrDataTrigger" />
				<ref bean="mailTrigger" /> -->
			</list>
		</property>
	</bean>
	
</beans>