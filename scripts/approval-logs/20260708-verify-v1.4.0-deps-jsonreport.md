# 审批日志：插件依赖拓扑排序 + JSON 标准化输出协议

**时间**：2026-07-08
**Commit**：35e5bef04d8cb54247c1db75b9489816f11dd767
**审批范围**：RULES.md §5.9-5.10 + verify-commit.sh v1.3.0 → v1.4.0 + PLUGIN_REGISTRY.md depends_on 列

## 审批请求

### ① 修改原因
用户提出 2 项补充修改意见：
1. 插件依赖关系的显式声明——需 `depends_on` 字段 + 拓扑排序 + 循环依赖检测
2. 验证结果的标准化输出协议——需 JSON Schema 规范 + 合并验证报告

不修改将导致：复杂验证链无法表达依赖关系（执行顺序错误）+ 插件输出格式散乱无法机器解析（无法接入监控看板）

### ② 变更 Diff 摘要
- RULES.md: 新增 §5.9 插件依赖关系（depends_on + Kahn 算法 + 循环依赖 + 失败传播矩阵）+ §5.10 JSON 输出协议（Schema + status 映射 + 合并报告 + 监控预留）（+150 行）
- scripts/verify-commit.sh: v1.3.0 → v1.4.0，新增 topo_sort_plugins + parse_plugin_json + generate_verify_report 三个函数；重写 run_plugins 实现拓扑排序执行 + 依赖失败传播 + JSON 收集合并（+220 行）
- scripts/verify-plugins/PLUGIN_REGISTRY.md: 表格新增 depends_on 列 + 依赖关系章节（拓扑排序规则 + 循环依赖检测 + 失败传播矩阵）（+35 行）
- scripts/verify-commit.sha256: 重新生成（sha256 变更）

### ③ 影响范围评估
- 影响所有子代理的 --pre 验证流程：插件按拓扑排序执行，依赖失败时跳过后续插件
- 影响 verify-commit.sh 输出：每次执行后生成 `scripts/logs/verify-report-<timestamp>.json` 合并报告
- 向后兼容：
  - 插件不输出 JSON 时自动按退出码生成标准 JSON（§5.10.3 兼容性）
  - depends_on=`-`（无依赖）时行为与 v1.3.0 完全一致
  - 现有 30-flyway-version-check.sh 无需修改即可工作
- 需通知 0 个活跃子代理（当前无活跃 worktree）

### ④ 回滚方案
- 回滚命令：`git revert <commit-hash>`
- 回滚后影响：verify-commit.sh 恢复 v1.3.0，无拓扑排序（按字母序执行），无 JSON 报告生成
- 回滚验证：`sha256sum -c scripts/verify-commit.sha256` 确认恢复 v1.3.0 的 sha256
- 回滚后清理：`rm -f scripts/logs/verify-report-*.json scripts/logs/plugin-results-*.jsonl`

### ⑤ 版本变更
- Verify-Version: verify-commit.sh 1.3.0 → 1.4.0
- 兼容性: 向后兼容（无 depends_on 配置时行为与 v1.3.0 一致；插件不输出 JSON 时自动生成）

### ⑥ 测试结果
- 语法检查: PASS（`bash -n scripts/verify-commit.sh`）
- sha256 完整性校验: PASS
- 单插件拓扑排序: PASS（30-flyway-version-check.sh，无依赖）
- 多插件拓扑排序: PASS（模拟 5 插件 2 依赖链，输出 01→02→03→10→20）
- 循环依赖检测: PASS（模拟 01→02→03→01 三环，正确返回 exit 1 + CYCLE 标识）
- 缺失依赖处理: PASS（依赖指向不存在插件时忽略，按 0 入度处理）
- JSON 输出解析: PASS（插件输出合法 JSON 时正确提取；无 JSON 时按退出码自动生成）
- 依赖失败传播: PASS（critical 插件失败时，依赖它的插件标记 SKIPPED）
- 合并验证报告: PASS（summary 字段 total/passed/failed/warned/timeout/skipped 计数正确）
- version/duration_ms 注入: PASS（从 PLUGIN_REGISTRY.md 读取 version，测量执行耗时）
- --skip-plugins 调试模式: PASS（正确跳过插件）
- 文件越权检测: PASS（正确识别受保护文件修改，exit 2）

## 用户批准
Approved-by: user on 2026-07-08（用户提出 2 项补充修改意见后批准实施）
