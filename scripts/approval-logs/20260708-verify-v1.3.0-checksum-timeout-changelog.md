# 审批日志：插件校验和 + 超时隔离 + CHANGELOG.json

**时间**：2026-07-08
**Commit**：待提交后填入
**审批范围**：RULES.md §5.6-5.8 + verify-commit.sh v1.2.0 → v1.3.0 + PLUGIN_REGISTRY.md 扩展 + CHANGELOG.json

## 审批请求

### ① 修改原因
用户提出 3 项补充修改意见：
1. 插件校验和的"更新同步"问题——需原子性同步 + 友好错误提示
2. 插件执行的"超时隔离"缺失——需根据 criticality/on_timeout 决定中断/降级
3. 变更日志的"机器可读性"不足——需结构化 CHANGELOG.json

不修改将导致：插件被篡改无法检测 + 插件超时无差别中断 + 主代理无法自动解析变更历史

### ② 变更 Diff 摘要
- RULES.md: 新增 §5.6 校验和同步 + §5.7 超时隔离 + §5.8 CHANGELOG.json（+140 行）
- scripts/verify-commit.sh: v1.2.0 → v1.3.0，新增 get_plugin_config + 校验和校验 + criticality/on_timeout 策略（+95 行）
- scripts/verify-plugins/PLUGIN_REGISTRY.md: 增加 timeout_sec/on_timeout/criticality/checksum 字段 + 组合策略矩阵 + 修改流程（+40 行）
- scripts/CHANGELOG.json: 新建，记录 7 个历史变更（+75 行）

### ③ 影响范围评估
- 影响所有子代理的 --pre 验证流程：插件执行前先校验 sha256，按配置决定中断/降级
- 影响 verify-commit.sh 退出码：critical 插件失败/超时返回 exit 1，optional 仅警告返回 exit 0
- 向后兼容：无 PLUGIN_REGISTRY.md 配置时默认 timeout=30/on_timeout=fail/criticality=critical
- 需通知 0 个活跃子代理（当前无活跃 worktree）

### ④ 回滚方案
- 回滚命令：`git revert <commit-hash>`
- 回滚后影响：verify-commit.sh 恢复 v1.2.0，不校验插件 sha256，无 criticality 策略
- 回滚验证：`sha256sum -c scripts/verify-commit.sha256` 确认恢复 v1.2.0 的 sha256

### ⑤ 版本变更
- Verify-Version: verify-commit.sh 1.2.0 → 1.3.0
- 兼容性: 向后兼容（无配置时行为与 v1.2.0 一致）

### ⑥ 测试结果
- JSON 合法性校验: PASS（jq . scripts/CHANGELOG.json）
- sha256 完整性校验: PASS
- --skip-plugins 调试模式: 正确跳过插件
- 文件越权检测: 正确识别受保护文件（exit 2）

## 用户批准
Approved-by: user on 2026-07-08（用户提出 3 项补充修改意见后批准实施）
